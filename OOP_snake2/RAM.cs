using System;
using System.Collections.Generic;
using System.Text;
using System.Threading;

namespace OOP_snake2
{
    class RAM
    {

        static void Ram(string[] args)
        {

       /*   
        *   All inside Program and Point.cs
        *   
        *   
        *   Point p1 = new Point(1, 3, '%');
            Move(p1, 10, 10);
            p1.Draw();


            Point p2 = new Point(4,5,'%');
            Reset(p2);
            p2.Draw();

            Console.ReadLine();
*/
        
       /* Example of RAM works 1
        
            int x = 1; // line in RAM
            int y = 3;
            Point p1 = new Point(); // sub-line in RAM. Extra line
            p1.x = 1;
            p1.y = 3;
            p1.sym = '*';

            Point p2 = new Point(4, 5, '%');
            p1 = p2; // That means what is p2 contains, the same will be in p1
            p2.x = 8;
            p2.y = 8;

         */
        }
    }
}
